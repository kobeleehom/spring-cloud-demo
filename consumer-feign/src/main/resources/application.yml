server:
  port: 8884

spring:
  application:
    name: consumer-feign
  #链路追踪配置
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8880/eureka/

feign:
  hystrix:
    enabled: true