server:
  port: 8883

spring:
  application:
    name: consumer-ribbon
  #链路追踪配置
  zipkin:
    base-url: http://localhost:9411

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8880/eureka/
